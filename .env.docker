# Django Settings
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION
DEBUG=False
ALLOWED_HOSTS=*

# Database Settings
# For Docker deployment
DB_NAME=attendansee_db
DB_USER=attendansee_user
DB_PASSWORD=attendansee_user
DB_HOST=postgres
DB_PORT=5432

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:53000,http://127.0.0.1:53000
CORS_ALLOW_ALL_ORIGINS=True

# Application Settings
DJANGO_SETTINGS_MODULE=attendansee_backend.settings
PYTHONUNBUFFERED=1

# Django Superuser Settings
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin123
DJANGO_SUPERUSER_EMAIL=admin@attendansee.local

# Gunicorn Settings (for image processing tasks)
GUNICORN_WORKERS=4
GUNICORN_TIMEOUT=300
GUNICORN_GRACEFUL_TIMEOUT=300

